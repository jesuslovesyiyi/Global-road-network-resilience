# Environment Setup

This folder contains all files needed to reproduce the computational environment used in this project.

## Conda Environment (Primary)

To recreate the main environment:

```bash
conda env create -f environment.yml
conda activate global0